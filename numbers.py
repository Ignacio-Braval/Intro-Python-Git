age = input("inserta tu edad: ")  # llamada para que el usuario interactue con el sistema

new_age = int(
    age) + 10  # convierte el dato que viene como string en un int para asi sumarlo con el 10, y lo guarda en una nueva variable

print(age)
